on:
  workflow_call:
    secrets:
      NPM_TOKEN:
        required: false
    inputs:
      checkout_modules:
        type: boolean
        required: false
      test_check:
        type: boolean
        required: false
      npm_registry:
        type: string
        required: false
        default: "https://npm.pkg.github.com"

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: checkout repository
        uses: actions/checkout@v7

      - name: prepare node
        uses: knallbumm/kb-github-actions/.github/actions/prepare-node@pnpm12
        with:
          npm_token: ${{ secrets.NPM_TOKEN }}
          checkout_modules: ${{ inputs.checkout_modules }}
          npm_registry: ${{ inputs.npm_registry }}

      - name: run build
        run: pnpm run build

      - name: save-to-cache
        uses: actions/cache/save@v6
        with:
          path: dist
          key: cache-${{ github.run_id }}-${{ github.run_attempt }}

  lint:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: checkout repository
        uses: actions/checkout@v7

      - name: prepare node
        uses: knallbumm/kb-github-actions/.github/actions/prepare-node@pnpm12
        with:
          npm_token: ${{ secrets.NPM_TOKEN }}
          checkout_modules: ${{ inputs.checkout_modules }}
          npm_registry: ${{ inputs.npm_registry }}

      - name: run linting
        run: pnpm run lint

  type:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: checkout repository
        uses: actions/checkout@v7

      - name: prepare node
        uses: knallbumm/kb-github-actions/.github/actions/prepare-node@pnpm12
        with:
          npm_token: ${{ secrets.NPM_TOKEN }}
          checkout_modules: ${{ inputs.checkout_modules }}
          npm_registry: ${{ inputs.npm_registry }}

      - name: run typecheck
        run: pnpm run typecheck

  test:
    runs-on: ubuntu-latest
    if: ${{ inputs.test_check == true }}
    timeout-minutes: 10
    steps:
      - name: checkout repository
        uses: actions/checkout@v7

      - name: prepare node
        uses: knallbumm/kb-github-actions/.github/actions/prepare-node@pnpm12
        with:
          npm_token: ${{ secrets.NPM_TOKEN }}
          checkout_modules: ${{ inputs.checkout_modules }}
          npm_registry: ${{ inputs.npm_registry }}

      - name: run tests
        run: pnpm run test
